export NETOP_NETWORK_TYPE="HostDeviceNetwork"       # network type
export NETOP_NETWORK_NAME="${NETOP_NETWORK_NAME:-hostdev-rdma-device}"     # secondary network base name
export NETOP_RESOURCE="${NETOP_RESOURCE:-rdma_device}"		    # resource base name
export NETOP_RESOURCE_PATH="nvidia.com"             # resource path for resource request
export NETOP_NETWORK_POOL="${NETOP_NETWORK_POOL:-hostdev-pool}"            # nvidia ipam pool name
