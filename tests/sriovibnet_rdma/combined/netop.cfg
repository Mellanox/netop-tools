export NETOP_NETWORK_TYPE="SriovIBNetwork"            # # network type
export NETOP_NETWORK_NAME="${NETOP_NETWORK_NAME:-sriovibnet-rdma}"           # secondary network base name
export NETOP_RESOURCE="${NETOP_RESOURCE:-sriovib_resource}"              # resource base name
export NETOP_RESOURCE_PATH="nvidia.com"               # resource path for resource request
export NETOP_NETWORK_POOL="${NETOP_NETWORK_POOL:-sriovibnet-pool}"           # nvidia ipam pool name
