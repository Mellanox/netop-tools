export NETOP_NETWORK_TYPE="SriovNetwork"       # network type
export NETOP_NETWORK_NAME="${NETOP_NETWORK_NAME:-sriovnet-dra}"       # secondary network base name
export NETOP_RESOURCE="${NETOP_RESOURCE:-sriov_resource}"         # resource base name
export NETOP_RESOURCE_PATH="nvidia.com"        # resource path for resource request
export NETOP_NETWORK_POOL="${NETOP_NETWORK_POOL:-sriovnet-pool}"      # nvidia ipam pool name
