export NETOP_NETWORK_TYPE="IPoIBNetwork"              # network type
export NETOP_NETWORK_NAME="${NETOP_NETWORK_NAME:-ipoib-rdma-shared-device}"  # secondary network base name
export NETOP_RESOURCE="${NETOP_RESOURCE:-rdma_shared_device}"            # resource base name
export NETOP_RESOURCE_PATH="rdma"                     # resource path for resource request
export NETOP_NETWORK_POOL="${NETOP_NETWORK_POOL:-ipoib-pool}"                # nvidia ipam pool name
